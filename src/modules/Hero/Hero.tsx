import bg from './images/bg.jpg';

const Hero = () => {
    return (
        <section id="hero" className="min-h-screen pt-[55px] bg-no-repeat bg-cover bg-center" style={{backgroundImage: `url(${bg})`}}>
            
        </section>
    );
};

export default Hero;